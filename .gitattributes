# Set the default behavior for all files to automatically normalize line endings and enforce LF.
* text=auto eol=lf

# Explicitly treat Python files as text files with normalized line endings.
*.py text eol=lf
*.pyx text eol=lf

# Mark common binary file types to ensure correct handling.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.bmp binary
*.ico binary
*.pdf binary
*.zip binary
*.tar binary
*.gz binary
*.rar binary
*.7z binary

# Configure Git LFS handling for large or special files (if you're using LFS).
# *.psd filter=lfs diff=lfs merge=lfs -text
# *.mp4 filter=lfs diff=lfs merge=lfs -text
# *.mov filter=lfs diff=lfs merge=lfs -text
# *.wav filter=lfs diff=lfs merge=lfs -text
# *.mp3 filter=lfs diff=lfs merge=lfs -text

# Example custom diff driver for Markdown files (requires additional Git config).
# *.md diff=markdown
