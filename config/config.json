/*
{
  // Directories that the file manager will monitor for new files.
  "directories_to_watch": [
    "C:/Users/User/Downloads", // Watches the Downloads folder for new files.
    "C:/Users/User/Documents"  // Watches the Documents folder for new files.
  ],

  // Rules for file movement based on file extensions.
  "rules": [
    {
      "target_dir": "C:/Users/User/Documents/PDFs", // Destination folder for PDFs.
      "extensions": [".pdf"] // Moves all .pdf files to the PDFs folder.
    },
    {
      "target_dir": "C:/Users/User/Documents/Images", // Destination folder for image files.
      "extensions": [".jpg", ".png"] // Moves .jpg and .png files to the Images folder.
    }
  ]
}
*/
